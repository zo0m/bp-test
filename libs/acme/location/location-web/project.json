{
    "name": "location-web",
    "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/acme/location/location-web/src",
    "projectType": "library",
    "tags": ["scope:acme", "domain:location", "platform:web"],
    "targets": {
        "test": {
            "executor": "@nx/vitest:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "passWithNoTests": true
            }
        }
    }
}
