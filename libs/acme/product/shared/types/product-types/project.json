{
    "name": "product-types",
    "$schema": "../../../../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/acme/product/shared/types/product-types/src",
    "projectType": "library",
    "tags": ["scope:acme", "domain:product", "platform:shared", "layer:types"],
    "targets": {
        "test": {
            "executor": "@nx/vitest:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "passWithNoTests": true
            }
        }
    }
}
