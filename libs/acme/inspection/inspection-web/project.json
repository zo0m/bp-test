{
    "name": "inspection-web",
    "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/acme/inspection/inspection-web/src",
    "projectType": "library",
    "tags": ["scope:acme", "domain:inspection", "platform:web"],
    "targets": {
        "test": {
            "executor": "@nx/vitest:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "passWithNoTests": true
            }
        }
    }
}
