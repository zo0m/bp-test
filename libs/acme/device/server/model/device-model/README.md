# @acme/device/server/device-model

Device model package.

## Artifacts

| Artifact       | Template       | Lifecycle |
| -------------- | -------------- | --------- |
| `server/model` | `server/model` | generated |

## Tags

`scope:acme` `domain:device` `platform:server` `layer:model`
