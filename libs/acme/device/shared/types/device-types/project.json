{
    "name": "device-types",
    "$schema": "../../../../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/acme/device/shared/types/device-types/src",
    "projectType": "library",
    "tags": ["scope:acme", "domain:device", "platform:shared", "layer:types"],
    "targets": {
        "test": {
            "executor": "@nx/vitest:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "passWithNoTests": true
            }
        }
    }
}
