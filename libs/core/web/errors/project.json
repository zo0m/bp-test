{
    "name": "core-web-errors",
    "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/core/web/errors/src",
    "projectType": "library",
    "tags": ["scope:core", "domain:core", "platform:web", "layer:errors"],
    "targets": {
        "test": {
            "executor": "@nx/vitest:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "passWithNoTests": true
            }
        }
    }
}
